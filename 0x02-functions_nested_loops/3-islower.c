#include "main.h"

/**
 * _islower - Checks if a character is lowercase.
 * Return (i) if lowercae and 0 otherwise.
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (i);
	else
		return (0);
}
